version: "3.9"

services:
  app:
    build: .
    container_name: money_tracker_backend
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - GIN_MODE=release
    restart: unless-stopped
